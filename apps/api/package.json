{
  "name": "@smsystem/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../src/config/secrets/.env.local --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  }
}
