import { PlanningPageClient } from "@/features/operational/components/planning-page-client";

export const metadata = { title: "Planning | SM-MIS" };

export default function PlanningPage() {
  return <PlanningPageClient />;
}
